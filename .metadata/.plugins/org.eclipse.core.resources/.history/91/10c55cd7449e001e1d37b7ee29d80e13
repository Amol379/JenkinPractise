import java.util.Arrays;
import java.util.Collections;
import java.util.Comparator;
import java.util.List;
import java.util.stream.Collectors;

public class Test {
	public static void main(String[] args) {

		List<Integer> asList = Arrays.asList(2,5,6,7,2,1,6,9);
		asList.stream().sorted(Integer::compareTo).collect(Collectors.toList());
		
		
		/*
		 * List<int[]> collect =
		 * Arrays.asList(arr).stream().sorted(Comparator.comparingInt(a -> (int)
		 * a).reversed()) .collect(Collectors.toList());
		 * collect.forEach(System.out::println);
		 */
		/*
		 * int temp = 0;
		 * 
		 * for (int i = arr.length-1; i > 0; i--) { System.out.println(i); temp =
		 * arr[i]; arr[i]= arr[i+1]; arr[i]= temp;
		 * 
		 * }
		 */
		System.out.println(arr[1]);
	}

}
